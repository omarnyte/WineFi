import React from "react"

import styles from "../styles/home.module.css"

const Home = () => (
  <div className={styles.container}>
    <h2 className={styles.content}>Find a boozy place to work!</h2>
  </div>
)

export default Home;