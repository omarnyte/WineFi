import { Link } from "gatsby"
import React from "react"

const NavBar = () => (
  <nav>
    <ul>
      <li>
        <Link to="/directory">Directory</Link>
      </li>
    </ul>
  </nav>
)

export default NavBar;

